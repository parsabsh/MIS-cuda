// typedef struct Vertex {
//     int degree;
//     int *Neighbors;
// } Vertex;

// typedef struct Graph {
//     int n;
//     Vertex *V;
// } Graph;

// void showoff();

// Graph* new_graph_device(int n, int max_degree);
// Graph* new_graph_host(int n, int max_degree);
// Graph* deep_copy_to_device(Graph *graph);

// __global__ void print_graph_dev(Graph* graph);

// void add_undirected_edge(Graph *graph, int v_i, int v_j);

// void print_graph(Graph graph);
